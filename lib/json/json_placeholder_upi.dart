import 'package:flutter/material.dart';

class JsonPlaceholderUpi extends StatelessWidget {
  const JsonPlaceholderUpi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          ElevatedButton(
            onPressed: () {},
            child: Text('D'),
          ),
        ],
      ),
    );
  }
}
